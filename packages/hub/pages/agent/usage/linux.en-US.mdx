# Linux

import {install} from "@site/src/cmd/install";
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import ServerMilkLogo from "@site/src/components/ServerMilkLogo/index";
import {getDesktopDownloadUrl, osEnum} from "@site/config";
import CodeBlock from '@theme/CodeBlock';
import {archEnum, moduleEnum} from "@site/config";
import daemonCMD from "@site/src/cmd/daemon";

## Command-line installation (suitable for systems without a graphical interface)

<details>
    <summary>Required to install wget, unzip</summary>
<Tabs>
<TabItem value="Debian" label="Debian、Ubuntu">

```bash
apt -y update && apt -y install wget unzip
```

</TabItem>
<TabItem value="CentOS" label="CentOS">

```bash
yum -y update && yum -y install wget unzip
```

</TabItem>
</Tabs>
</details>

Choose either *deploy* or *web* for installation

- *deploy* (includes web features and provides additional **interactive installation**, **startup on boot**, **automatic updates**, etc.)
- *web* (requires manual startup and manual updates)

### Install deploy (**Recommended**)

:::tip
Copy and execute line by line or copy and execute all at once
:::

<Tabs groupId="architecture">
    <TabItem value="amd" label="x86_64"><CodeBlock language="bash">{install(osEnum.linux, archEnum.x86, moduleEnum.deploy)}</CodeBlock></TabItem>
    <TabItem value="arm" label="arrch64"><CodeBlock language="bash">{install(osEnum.linux, archEnum.arm, moduleEnum.deploy)}</CodeBlock></TabItem>
</Tabs>

#### Interactive Installation
![](/img/interactive.gif)

#### Set Up Autostart

☑️ Checking the autostart option will output logs as shown in the image

Copy the command within the red frame, and execute it.
![linux-daemon](https://assets.serverbee.app/docs/linux-daemon.jpg)

<details>
    <summary>The specific code is here</summary>
    <CodeBlock language="bash" >{daemonCMD}</CodeBlock>
</details>

### Install web

<Tabs groupId="architecture">
    <TabItem value="amd" label="x86_64"><CodeBlock language="bash">{install(osEnum.linux, archEnum.x86, moduleEnum.web)}</CodeBlock></TabItem>
    <TabItem value="arm" label="arrch64"><CodeBlock language="bash">{install(osEnum.linux, archEnum.arm, moduleEnum.web)}</CodeBlock></TabItem>
</Tabs>

:::info Custom port number
```bash
./serverbee-web -p 9528
```
:::

## Linux Desktop Environment
<ServerMilkLogo />

<Tabs>
    <TabItem value="amd" label="x86_64">
        <a href={getDesktopDownloadUrl(osEnum.linux)}>👉 Click to download AppImage mirror</a>
    </TabItem>
</Tabs>
