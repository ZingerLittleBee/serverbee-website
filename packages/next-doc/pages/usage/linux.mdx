---
title: Linux
sidebar_position: 1
---

import {install} from "@/cmd/install";
import ServerMilkLogo from "@/components/ServerMilkLogo/index";
import {getDesktopDownloadUrl, osEnum} from "@/config";
import {archEnum, moduleEnum} from "@/config";
import {Tabs, Tab, Callout, Code, Pre, CopyToClipboard} from 'nextra/components'
import Button from '@/components/Button'

## Linux 桌面环境
<ServerMilkLogo />

<Tabs items={['x86_64']}>
    <Tab>
        <Button target="_parent" url={getDesktopDownloadUrl(osEnum.linux)}>
            👉 点击下载 AppImage 镜像
        </Button>
    </Tab>
</Tabs>

## 命令行安装 (适合无图形系统使用)

<details>
    <summary>需要安装 wget、unzip</summary>

    <Tabs items={['Debian、Ubuntu', 'CentOS']}>
        <Tab>
            ```bash copy
            apt -y update && apt -y install wget unzip
            ```
        </Tab>
        <Tab>
            ```bash copy
            yum -y update && yum -y install wget unzip
            ```
        </Tab>
    </Tabs>
</details>

*deploy* 和 *web* **任选其一**安装即可

- *deploy*（包含 web 功能，并额外提供**交互式安装**、**开机自启**、**自动更新**等功能）
- *web*（需要手动启动、手动更新）

### 安装 deploy（**推荐**）

:::tip
逐行复制执行或者全部复制执行
:::

<Tabs items={['x86_64', 'arm64']}>
    <Tab>
        {install(osEnum.linux, archEnum.x86, moduleEnum.deploy)}
    </Tab>
    <Tab>{install(osEnum.linux, archEnum.arm, moduleEnum.deploy)}</Tab>
</Tabs>

#### 交互式安装
![](/img/interactive.gif)

#### 设置自启动

☑️ 勾选自启动会输出如图日志

复制红框内的命令，执行即可
![linux-daemon](https://assets.serverbee.app/docs/linux-daemon.jpg)

<details>
    <summary>具体代码在这里</summary>

```bash copy {9}
cat > /etc/systemd/system/serverbee-deploy.service <<EOF
[Unit]
Description=ServerBee Deploy
After=network-online.target systemd-resolved.service
Wants=network-online.target systemd-resolved.service

[Service]
Type=oneshot
ExecStart=/root/serverbee-deploy # 修改为实际路径
RemainAfterExit=yes
Environment="RUST_LOG=info"

[Install]
WantedBy=multi-user.target
EOF
systemctl enable serverbee-deploy.service
```

</details>

### 安装 web

<Tabs items={['x86_64', 'arm64']}>
    <Tab>
        {install(osEnum.linux, archEnum.x86, moduleEnum.web)}
    </Tab>
    <Tab>{install(osEnum.linux, archEnum.arm, moduleEnum.web)}</Tab>
</Tabs>

<Callout type="info" emoji="ℹ️">
    自定义端口号
</Callout>

```bash copy
./serverbee-web -p 9528
```
